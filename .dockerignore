.git
.gitignore
.venv
venv
__pycache__
**/__pycache__
*.py[cod]
.pytest_cache
.ruff_cache
.mypy_cache
.env
.env.*
!.env.example
data/
*.db
*.db-wal
*.db-shm
# The docs and the vault are deliverables that tests/test_documentation.py asserts on, so
# they must be in the build context. Only docker/tests.Dockerfile copies them; the two
# service images copy their own source and nothing else.
*.md
!README.md
!CLAUDE.md
.github/
